const money = 10000,
      income = 5000,
      addExpenses = '10%',
      deposit = 20000,
      mission = 'slave the world',
      period = 'untill tomorrow';

alert('enslaved world');

console.log(`I got ${deposit} dollars for ${mission}. But it doesn't enough.`);


